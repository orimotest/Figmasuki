# Final Candidates

背景3案は比較用の素材で終わらせず、Final A / B / C の3枚の完成候補としてFigmaへ出力します。

- `finalCandidate` は従来互換の代表1案として残す
- `finalCandidates` がある場合、UIとFigma出力では3案を優先する
- Final Aは落ち着いた導入、Final Bは構造感、Final Cは編集感のように、背景ごとの個性を判断材料として残す
- Figma上では各Finalを横並びで配置し、背景レイヤーとSVG/テキストレイヤーを分けて後から調整できる状態にする
